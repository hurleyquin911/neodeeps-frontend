import { AdminModerationClient } from "@/components/dashboard/admin/AdminModerationClient";

export const metadata = { title: "Admin — Antrian Moderasi" };

export default function AdminModerationPage() {
  return <AdminModerationClient />;
}
