import { CreateEventClient } from "@/components/dashboard/user/CreateEventClient";

export const metadata = {
  title: "Buat acara – Neodeeps",
};

export default function CreateEventPage() {
  return <CreateEventClient />;
}
