import { GlobalFeedClient } from "@/components/dashboard/user/GlobalFeedClient";

export const metadata = { title: "Beranda — Neodeeps" };

export default function UserDashboardPage() {
  return <GlobalFeedClient />;
}
