import { CreateCommunityClient } from "@/components/dashboard/user/CreateCommunityClient";

export const metadata = { title: "Buat komunitas – Neodeeps" };

export default function CreateCommunityPage() {
  return <CreateCommunityClient />;
}
